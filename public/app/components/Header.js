
import React, { PropTypes, Component } from 'react'

const HeaderView = (props) => {
    return (
        <h2> Hello friend !  </h2>
    )
};

const Header = (props) => {

    return (
       <HeaderView/>
    )
};

export default Header;